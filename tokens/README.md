# Design Tokens

Drop `colors.json` and `typography.json` here. Once added, `app/globals.css` will be wired
to reference them as CSS custom properties (e.g. `--color-neutral-fg1`, `--font-size-title3`)
following the shadcn CSS-variable pattern, resolving correctly in both light and dark themes.
